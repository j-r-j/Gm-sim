/**
 * Core game logic module
 *
 * This module contains all the simulation mechanics, data models,
 * and game engine logic. These internals are intentionally hidden
 * from the UI layer - players only see results, never the underlying
 * mechanics or ratings.
 */

export * from './models';
export * from './engine';
